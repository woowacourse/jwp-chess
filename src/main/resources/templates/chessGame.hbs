<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link href="../css/chessStyle.css" rel="stylesheet" type="text/css"/>
    <title>크리스마스 체스</title>
</head>

<body>
<div id="gameUI">
    <h1 id="title">크리스마스 체스</h1>
    <h2 id="roomName">{{moveResponse.roomName}}</h2>
    <div id="chessBoard">
        {{#moveResponse.chessPieces}}
            <div id="{{position}}" class="tile">
                {{#if piece}}
                    <img alt="position" class="tileImage" src="../images/{{piece}}.png"/>
                {{else}}
                    <img alt="empty" src="../images/blank.png"/>
                {{/if}}
            </div>
        {{/moveResponse.chessPieces}}
    </div>

    <div id="commandUI">
        <div id="current_team">{{moveResponse.currentTeam}}팀 차례입니다.</div>
        {{#if moveResponse.currentTeamScore}}
            <div id="chess_score">{{moveResponse.currentTeam}}팀의 현재 점수는 {{moveResponse.currentTeamScore}}점 입니다.</div>
        {{/if}}

        <form action="/end/{{moveResponse.roomId}}" method="get">
            <button>저장 후 종료하기</button>
        </form>
    </div>
</div>
<script type="text/javascript" src="../js/chess.js"></script>
</body>

</html>