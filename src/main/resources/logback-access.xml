<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%n=== HTTP REQUEST === %n%fullRequest === HTTP RESPONSE === %n%fullResponse%n</pattern>
        </encoder>
    </appender>

    <appender-ref ref="CONSOLE"/>
</configuration>
