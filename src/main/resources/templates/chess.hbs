<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
    <title>Chess Game</title>
</head>
<body>
<h1 style="text-align: center; margin-bottom: 50px;">체스게임 가보자고!</h1>
<div id="container">
    <div id="chessboard">
    <div id="chessboard-container">
		{{#pieces}}
			<div id="{{position}}" class="square">
				<div class="draggable" draggable="true">
					{{#if symbol}}
						<img src="/css/images/{{team}}-{{symbol}}.svg" class="piece"/>
					{{/if}}
				</div>
			</div>
		{{/pieces}}
    </div>

    </div>
    <div class="buttons-container">
        <div class="btn-group">
            <button type="submit" class="btn btn-success" onclick = "makeForm('{{chessGameId}}', 'status')">점수</button>
            <button type="submit" class="btn btn-danger" onclick = "makeForm('{{chessGameId}}', 'end')">게임 종료</button>
            <form action="/">
            	<input type="submit" value="로비로 가기" class="btn btn-secondary">
			</form>
        </div>
        <div class="info">
            <div>
                {{#if error}}
                    <h3>에러 메시지: {{error}}</h3>
                {{/if}}
                {{#if whiteScore}}
                    <h3>흰팀 점수: {{whiteScore}}</h3>
                {{/if}}

                {{#if blackScore}}
                    <h3>검은팀 점수: {{blackScore}}</h3>
                {{/if}}

                {{#if winTeam}}
                    <h3>승자: {{winTeam}}</h3>
                {{/if}}
            </div>
        </div>
    </div>
</div>
<script src="/js/app.js"></script>
</body>
</html>
